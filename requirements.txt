# FinAuditPro Production Dependencies (Target Runtime: Python 3.12)
# Default install profile includes full Core + AI stack.
# For lightweight Core-only installation (CRM, Working Papers, Reports), run:
#   pip install -r requirements-core.txt

-r requirements-ai.txt


