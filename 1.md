FINAUDITPRO — COMPLETE CODEBASE AUDIT, VERIFICATION, REFACTORING & UI/UX
OVERHAUL You are acting as the principal software architect, senior full-stack
engineer, security engineer, QA engineer, DevOps engineer, product designer, and
UI/UX engineer responsible for taking my FinAuditPro software from its current
implementation to a production-grade, professional, secure, reliable,
maintainable, polished application. Your job is NOT to simply give me
suggestions. You must inspect the actual codebase deeply, understand how
everything works, identify problems, verify assumptions, test functionality
wherever possible, fix issues, improve architecture where justified, and improve
the UI/UX directly in the codebase. Treat this as a serious software engineering
engagement.

1. PRIMARY OBJECTIVE Perform a complete technical and product audit of my entire
   FinAuditPro application. You must determine:
2. Whether the application actually works correctly.
3. Whether the architecture is sound.
4. Whether frontend and backend communicate correctly.
5. Whether APIs are correctly implemented.
6. Whether database interactions are correct.
7. Whether authentication and authorization are secure.
8. Whether financial/audit calculations are correct.
9. Whether edge cases are handled.
10. Whether errors are handled properly.
11. Whether data validation is sufficient.
12. Whether sensitive information is protected.
13. Whether the code contains security vulnerabilities.
14. Whether there are race conditions or state-management problems.
15. Whether there are performance bottlenecks.
16. Whether the application can scale reasonably.
17. Whether the UI is professional and consistent.
18. Whether the UX is intuitive.
19. Whether accessibility is acceptable.
20. Whether responsive behavior works.
21. Whether the application feels like a serious professional financial/audit
    product rather than a prototype.
22. Whether there is duplicated, obsolete, dead, or unnecessarily complicated
    code.
23. Whether dependencies are appropriate and safely used.
24. Whether configuration/environment handling is correct.
25. Whether logging and monitoring are appropriate.
26. Whether tests are sufficient.
27. Whether deployment/build configuration is correct.

Then: FIX THE PROBLEMS YOU FIND. Do not stop at identifying problems unless a
change would be dangerous or requires information that genuinely cannot be
determined from the codebase. 2. IMPORTANT OPERATING PRINCIPLE Do NOT assume
that existing code is correct simply because it already exists. Do NOT preserve
bad architecture merely because it is already implemented. Do NOT rewrite
everything unnecessarily either. Use engineering judgment. For every major
change, ask: Does this make FinAuditPro more correct, secure, maintainable,
performant, testable, and professional without unnecessarily increasing
complexity? If yes, implement it. If no, leave the existing implementation
alone. Prefer:

- correctness
- security
- maintainability
- simplicity
- consistency
- testability
- performance
- accessibility
- professional UX

over unnecessary cleverness. 3. FIRST STEP — UNDERSTAND THE ENTIRE PROJECT
Before modifying anything, inspect the entire repository. Do not immediately
start editing files. First understand:

- project structure
- frontend
- backend
- API layer
- database
- authentication
- authorization
- state management
- routing
- components
- services
- utilities
- configuration
- environment variables
- build system
- package/dependency management
- tests
- scripts
- deployment configuration
- documentation
- assets
- design system
- existing UI patterns

Build a mental model of the entire application. Identify: Frontend

- framework
- routing
- component architecture
- state management
- data fetching
- forms
- validation
- error handling
- loading states
- caching
- authentication state
- permissions
- charts
- tables
- dashboards
- navigation
- responsive behavior

Backend

- framework
- controllers/routes
- services
- middleware
- authentication
- authorization
- validation
- business logic
- database access
- background jobs
- file processing
- integrations
- logging
- error handling

Database Determine:

- database technology
- schema
- relationships
- indexes
- constraints
- migrations
- transactions
- data types
- nullable fields
- uniqueness rules
- foreign keys
- audit trails
- soft deletion
- historical data handling

Infrastructure Inspect:

- environment configuration
- secrets handling
- build scripts
- deployment
- Docker if present
- CI/CD if present
- production configuration
- development configuration

4. DO NOT TRUST DOCUMENTATION OVER CODE If documentation says one thing but the
   code does another: trust the actual implementation first. Identify the
   discrepancy. Likewise:

- comments may be outdated
- README instructions may be outdated
- types may not reflect runtime behavior
- API documentation may be stale
- UI may expose functionality that backend no longer supports

Verify everything against the actual implementation. 5. CREATE A COMPLETE SYSTEM
MAP Before major modifications, internally construct a map of:

```text
User
 ↓
Authentication
 ↓
Frontend
 ↓
State Management
 ↓
API Client
 ↓
Backend Routes
 ↓
Middleware
 ↓
Business Logic
 ↓
Database
 ↓
External Services
```

Understand every important data flow. For each critical feature determine:

```text
Input
→ Validation
→ Transformation
→ Business Logic
→ Persistence
→ Response
→ Frontend State
→ UI Rendering
```

Look for inconsistencies between these layers. 6. FUNCTIONAL AUDIT Audit every
major feature. Do not limit yourself to obvious happy paths. For each feature
test mentally or through available tooling: Happy path

- Does it work?

Invalid input

- What happens?

Empty input

- What happens?

Missing data

- What happens?

Duplicate data

- What happens?

Unauthorized user

- What happens?

Unauthenticated user

- What happens?

Expired session

- What happens?

Network failure

- What happens?

Server failure

- What happens?

Database failure

- What happens?

Concurrent operations

- What happens?

Large datasets

- What happens?

Very long strings

- What happens?

Special characters

- What happens?

Unexpected types

- What happens?

Partial failure

- What happens?

Browser refresh

- What happens?

Back button

- What happens?

Multiple tabs

- What happens?

Mobile viewport

- What happens?

Every important workflow should fail gracefully. 7. FINANCIAL / AUDIT LOGIC
VERIFICATION FinAuditPro is a financial/audit-oriented application. Treat
calculations and financial logic as HIGH IMPORTANCE. Do not assume calculations
are correct. Inspect:

- totals
- subtotals
- balances
- variances
- percentages
- ratios
- margins
- financial periods
- dates
- rounding
- currency
- decimals
- negative values
- zero values
- null values
- tax calculations if present
- adjustments
- reconciliation
- aggregation
- reporting
- audit findings
- risk scores
- financial metrics

Look specifically for: Floating point problems Do not casually use binary
floating-point arithmetic for monetary values where exact decimal arithmetic is
required. Determine whether the application should use:

- integer minor units
- decimal/numeric database types
- Decimal libraries
- properly controlled rounding

depending on the architecture. Rounding Verify:

- when rounding occurs
- where rounding occurs
- whether rounding happens before or after aggregation
- whether displayed values differ from stored values
- whether frontend and backend calculations disagree

Currency Check:

- currency codes
- currency symbols
- formatting
- currency conversion
- precision
- localization

Dates Check:

- timezone handling
- UTC/local conversion
- financial periods
- month boundaries
- year boundaries
- leap years
- fiscal years

Percentages Check:

- division by zero
- negative denominators
- missing values
- precision
- formatting

If you discover a financial calculation that is potentially incorrect,
investigate it thoroughly before modifying it. 8. SECURITY AUDIT Perform a
serious application security review. Think like a defensive application security
engineer. Check for: Authentication

- weak authentication flows
- session problems
- token storage
- token expiration
- refresh token handling
- logout behavior
- session fixation
- insecure persistence
- password handling
- password reset flows
- account enumeration

Authorization Verify authorization server-side. Never trust frontend
restrictions. Check:

- role checks
- permission checks
- ownership checks
- tenant isolation if applicable
- resource-level authorization
- IDOR vulnerabilities
- privilege escalation

Example:

```text
GET /api/reports/123
```

must not allow a user to access report 123 merely because they know the ID.
Injection Check for:

- SQL injection
- NoSQL injection
- command injection
- template injection
- XSS
- HTML injection
- path traversal

XSS Inspect:

- dangerouslySetInnerHTML
- raw HTML rendering
- markdown rendering
- user-generated content
- URL handling
- rich text
- error messages
- imported data

CSRF Determine whether CSRF protection is necessary based on the authentication
architecture. SSRF Inspect:

- URL fetching
- external integrations
- webhook functionality
- user-controlled URLs

File uploads If present, inspect:

- file type validation
- MIME validation
- extension validation
- file size
- filename sanitization
- storage
- executable content
- path traversal
- malicious documents

Secrets Search for:

- API keys
- passwords
- tokens
- credentials
- private keys
- hardcoded secrets

Never expose secrets in frontend bundles. Sensitive data Identify:

- financial information
- user information
- credentials
- audit records
- documents
- personally identifiable information

Determine whether sensitive data is unnecessarily exposed. Logging Ensure
secrets and sensitive financial information are not unnecessarily logged. 9. API
AUDIT Inspect every API endpoint. For each endpoint verify:

- HTTP method
- authentication
- authorization
- input validation
- output validation
- error handling
- status codes
- pagination
- filtering
- sorting
- rate limiting where appropriate
- consistency
- idempotency where appropriate
- transaction safety

Look for APIs that:

- accept too much data
- expose too much data
- trust client-side values
- lack validation
- use inconsistent response formats
- leak internal errors
- return sensitive fields
- have inconsistent error codes

Make the API behavior predictable and consistent. 10. DATABASE AUDIT Inspect the
database architecture carefully. Check:

- schema correctness
- relationships
- constraints
- indexes
- query efficiency
- N+1 queries
- unnecessary queries
- missing transactions
- race conditions
- uniqueness
- foreign keys
- cascading behavior
- deletion behavior
- migrations
- data integrity

Look for cases where:

```text
read → modify → write
```

can create race conditions. Use transactions or atomic database operations where
appropriate. 11. PERFORMANCE AUDIT Find performance problems. Inspect: Frontend

- unnecessary rerenders
- large component trees
- excessive state updates
- expensive calculations
- unnecessary API requests
- duplicate requests
- poor caching
- oversized bundles
- large images
- unnecessary dependencies
- rendering huge tables
- charts with excessive data
- missing virtualization

Backend

- N+1 queries
- slow database queries
- unnecessary serialization
- expensive loops
- repeated external API requests
- synchronous blocking operations
- excessive memory usage

Network Check:

- payload size
- API request frequency
- caching
- compression
- pagination
- lazy loading

Do not prematurely optimize. Only introduce complexity where there is a real
benefit. 12. CODE QUALITY AUDIT Inspect the code for:

- duplicated logic
- giant functions
- giant components
- unclear names
- inconsistent naming
- unnecessary abstractions
- overengineering
- underengineering
- dead code
- unreachable code
- unused imports
- unused dependencies
- stale comments
- magic numbers
- magic strings
- inconsistent error handling
- inconsistent types
- excessive any usage
- weak typing
- circular dependencies

Refactor where appropriate. Do not perform meaningless stylistic changes just to
make the diff large. 13. TYPE SAFETY If the project uses TypeScript or another
typed language: Audit:

- any
- unknown
- unsafe casts
- nullable values
- API types
- database types
- form types
- state types
- component props
- return types

Ensure frontend and backend types don't silently disagree. Do not use:

```ts
as any
```

as a lazy way to make errors disappear. Fix the underlying type problem. 14.
ERROR HANDLING Make error handling professional. The application should
distinguish between:

```text
Validation error
Authentication error
Authorization error
Not found
Conflict
Rate limit
Network error
Server error
Database failure
Unexpected error
```

Users should receive useful messages. Developers should receive useful
diagnostic information. Do NOT expose:

- stack traces
- SQL errors
- internal implementation details
- secrets
- internal infrastructure information

to end users. 15. LOADING STATES Every asynchronous UI interaction should have
an appropriate state. Inspect:

- initial loading
- background loading
- button loading
- table loading
- page loading
- skeletons
- empty states
- error states
- retry states

Avoid interfaces that appear frozen while an API call is running. Prevent
accidental double submissions. 16. EMPTY STATES Every important empty state
should be intentionally designed. Examples:

```text
No audits yet
No reports found
No transactions available
No search results
No findings
No notifications
No recent activity
```

Do not leave users staring at blank panels. Explain:

1. what is empty
2. why it might be empty
3. what the user can do next
4. UI/UX OVERHAUL Now perform a serious UI/UX review of FinAuditPro. The product
   should visually communicate: professional financial software + modern SaaS +
   audit/compliance credibility. Avoid making it look like:

- a generic admin dashboard
- a student project
- a template
- a flashy crypto dashboard
- an over-designed consumer app

The interface should feel:

- trustworthy
- precise
- clean
- modern
- professional
- information-dense without being overwhelming
- consistent
- calm
- efficient
- enterprise-ready

18. DESIGN SYSTEM Create or improve a consistent design system. Standardize:
    Typography Define:

- font family
- heading hierarchy
- body text
- labels
- captions
- numeric data
- table typography

Spacing Use a consistent spacing scale. Avoid arbitrary margins everywhere.
Radius Use consistent border radii. Shadows Use subtle shadows only where they
improve hierarchy. Borders Use consistent border treatment. Colors Establish
semantic colors for:

- primary
- secondary
- success
- warning
- danger
- info
- neutral
- background
- surface
- border
- text

Financial applications should use color carefully. Do not rely on color alone to
communicate meaning. 19. DASHBOARD DESIGN If FinAuditPro has a dashboard,
improve it substantially. The dashboard should answer: What is happening? What
needs attention? What changed? What is risky? What action should I take?
Prioritize information. Possible structure:

```text
Top Navigation
────────────────────────────────

Page Title + Context + Actions

Key Metrics
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ Metric │ │ Metric │ │ Metric │ │ Metric │
└────────┘ └────────┘ └────────┘ └────────┘

Risk / Attention Area

Charts / Trends

Recent Activity

Audit Findings / Tasks

Additional Details
```

Do not blindly implement this exact structure. Use the existing application's
actual features. 20. TABLE UX Financial/audit applications often depend heavily
on tables. Improve tables with:

- clear headers
- alignment appropriate for data types
- numeric alignment
- currency formatting
- percentage formatting
- row hover states
- pagination
- sorting
- filtering
- search
- column visibility where useful
- responsive handling
- empty states
- loading states
- error states
- row actions
- keyboard accessibility

Do not make tables unnecessarily decorative. Prioritize scanability. 21. FORMS
Audit all forms. Improve:

- labels
- required indicators
- input formatting
- validation
- inline errors
- error summaries
- keyboard navigation
- focus states
- disabled states
- loading states
- success feedback

Validation should happen appropriately on both:

```text
Client
+
Server
```

Never trust client-side validation alone. 22. NAVIGATION Review the navigation
architecture. Users should quickly understand:

- where they are
- where they can go
- what is important
- what requires attention

Improve:

- sidebar
- top bar
- breadcrumbs where useful
- active states
- nested navigation
- mobile navigation

Avoid excessive navigation complexity. 23. RESPONSIVE DESIGN Test the UI
conceptually and through available tooling at:

- 320px
- 375px
- 390px
- 430px
- 768px
- 1024px
- 1280px
- 1440px
- 1920px

Check:

- overflow
- tables
- charts
- navigation
- modals
- forms
- cards
- buttons
- typography
- spacing

Do not simply shrink desktop layouts. Design proper responsive behavior. 24.
ACCESSIBILITY Audit for:

- semantic HTML
- keyboard navigation
- focus indicators
- screen reader labels
- ARIA only where necessary
- color contrast
- form labels
- error announcements
- modal accessibility
- button semantics
- link semantics
- heading hierarchy

Aim for WCAG-conscious implementation. 25. DARK MODE If the application already
supports dark mode: Audit it completely. Do not simply invert colors. Check:

- contrast
- charts
- borders
- cards
- inputs
- tables
- dialogs
- alerts
- disabled states
- hover states

If dark mode does not exist but the architecture makes it reasonable, consider
whether implementing it would improve the product. Do not introduce it solely
for novelty. 26. MICROINTERACTIONS Use subtle interactions where useful:

- hover
- focus
- button feedback
- loading
- success
- error
- transitions
- modal animation
- navigation transitions

Avoid excessive animation. Financial software should feel stable and
controlled. 27. DATA VISUALIZATION If charts exist, audit them. Charts must be:

- accurate
- readable
- appropriately labeled
- correctly scaled
- responsive
- accessible
- consistent

Do not use a chart merely because there is space for one. Every visualization
should answer a useful question. 28. SECURITY + UX BALANCE Do not sacrifice
security for convenience. Examples: Bad:

```text
Hide authorization logic only in frontend.
```

Correct:

```text
Frontend hides inaccessible actions
+
Backend enforces authorization.
```

Bad:

```text
Show raw server error.
```

Correct:

```text
Show useful user-facing error
+
log technical diagnostic information safely.
```

29. TESTING Inspect existing tests. Determine:

- what is tested
- what is not tested
- whether tests are meaningful
- whether tests test implementation details unnecessarily
- whether critical business logic has coverage

Prioritize tests for: Critical financial calculations Authentication
Authorization API endpoints Database operations Important workflows Forms
Critical UI interactions Add tests where practical. Do not create meaningless
tests merely to increase coverage percentages. 30. DEPENDENCY AUDIT Inspect
dependencies. Look for:

- unused dependencies
- duplicated libraries
- obsolete packages
- unnecessary packages
- questionable packages
- security risks
- version incompatibilities

Do not upgrade everything blindly. Only upgrade dependencies when justified. If
a dependency upgrade creates breaking changes, understand and address them
properly. 31. ENVIRONMENT CONFIGURATION Audit:

```text
.env
.env.example
development config
production config
build config
runtime config
```

Make sure:

- secrets aren't committed
- frontend doesn't receive backend secrets
- required environment variables are documented
- missing variables fail clearly
- defaults are safe

32. LOGGING Logging should be useful. Use appropriate log levels. Avoid:

```text
console.log("password", password)
console.log(user)
console.log(token)
```

Never log secrets. Do not expose sensitive financial information
unnecessarily. 33. OBSERVABILITY If appropriate for the current architecture,
identify opportunities for:

- structured logging
- request IDs
- error tracking
- performance monitoring
- audit logs
- health checks

Do not add unnecessary infrastructure if the application does not need it
yet. 34. AUDIT TRAIL / FINANCIAL INTEGRITY Because this is FinAuditPro, consider
whether important actions should have an immutable or traceable history.
Potential events:

- report created
- report edited
- audit created
- finding created
- finding resolved
- financial data changed
- user permission changed
- export generated
- document uploaded

If the product architecture requires an audit trail, ensure it is:

- reliable
- timestamped
- associated with the actor
- resistant to casual modification
- queryable

Do not invent compliance requirements that are not applicable. 35. FILE AND
DOCUMENT HANDLING If FinAuditPro handles documents: Audit:

- upload
- download
- preview
- parsing
- extraction
- storage
- permissions
- deletion
- versioning
- metadata
- file size
- file type
- malicious files
- path traversal
- access control

Users must never be able to access another user's private documents simply by
changing an ID. 36. SEARCH If search exists, verify:

- relevance
- case sensitivity
- special characters
- empty queries
- large queries
- pagination
- filtering
- performance
- authorization

Search must not bypass permissions. 37. EXPORTS / REPORTS If the application
generates:

- CSV
- Excel
- PDF
- financial reports
- audit reports

verify:

- data accuracy
- formatting
- permissions
- filename handling
- injection risks
- large dataset behavior
- consistency with on-screen data

CSV exports must be reviewed for spreadsheet formula injection if
user-controlled content can enter cells. 38. API DATA CONTRACTS Check for
inconsistencies such as: Backend:

```json
{
  "created_at": "..."
}
```

Frontend expecting:

```json
{
  "createdAt": "..."
}
```

Identify and fix these contract mismatches. Prefer a consistent API
convention. 39. STATE MANAGEMENT Inspect global and local state. Identify:

- unnecessary global state
- duplicated state
- derived state stored unnecessarily
- stale state
- race conditions
- optimistic update bugs
- cache invalidation problems

Avoid putting everything into global state. 40. CONCURRENCY Look for race
conditions involving:

- saving
- editing
- deleting
- refreshing
- uploading
- submitting
- fetching
- optimistic updates

Example:

```text
User changes record
 ↓
Request A starts

User changes record again
 ↓
Request B starts

B completes
 ↓
A completes later

Old data overwrites new data
```

Protect against this where relevant. 41. DATA VALIDATION Validation should exist
at appropriate boundaries. Validate:

- types
- ranges
- required fields
- string lengths
- enum values
- dates
- currency
- IDs
- uploaded files
- nested objects

Do not trust:

- browser input
- request bodies
- query parameters
- URL parameters
- hidden fields
- client-calculated values

42. BUSINESS LOGIC LOCATION Do not allow critical business logic to be
    duplicated across frontend and backend unnecessarily. For example:

```text
Frontend calculates total
Backend calculates total differently
```

This creates correctness problems. Identify the authoritative source of
truth. 43. UX COPY Audit user-facing copy. Fix:

- vague errors
- confusing labels
- inconsistent terminology
- grammar
- awkward wording
- developer terminology
- inconsistent capitalization

Use terminology consistently across the application. For example, don't call the
same concept:

```text
Audit
Review
Assessment
Inspection
```

unless these are actually different concepts. 44. PROFESSIONAL PRODUCT FEEL Look
for details that make the application feel unfinished:

- inconsistent spacing
- random colors
- inconsistent icons
- awkward empty space
- cramped sections
- oversized headings
- tiny buttons
- inconsistent cards
- unnecessary gradients
- inconsistent border radius
- poor loading states
- abrupt transitions
- inconsistent terminology
- generic placeholder text
- broken mobile layouts

Fix these. The goal is: “This looks like a serious professional financial/audit
SaaS product.” not: “This looks like an AI-generated dashboard template.” 45. DO
NOT OVERDESIGN Avoid:

- excessive gradients
- excessive glassmorphism
- giant hero sections
- meaningless animations
- huge typography
- excessive shadows
- unnecessary rounded cards
- decorative charts
- excessive colors

Professional financial software benefits from clarity more than visual
spectacle. 46. ICONOGRAPHY Use one coherent icon system. Do not mix random icon
styles. Icons should communicate meaning rather than decorate every element.
Ensure icon-only controls have accessible labels. 47. MODALS / DIALOGS Audit
dialogs. Check:

- focus trapping
- escape behavior
- background interaction
- loading state
- error state
- confirmation
- destructive actions
- responsive behavior

Destructive actions should have clear consequences. 48. NOTIFICATIONS / TOASTS
Use notifications intentionally. Good:

```text
Audit created successfully.
Report exported successfully.
Changes saved.
```

Bad:

```text
Success!
```

Errors should explain what the user can do. Avoid notification spam. 49.
DESTRUCTIVE ACTIONS For:

- delete
- archive
- revoke
- remove
- reset
- permanently modify

ensure the UX is intentional. If irreversible:

- clearly communicate the consequence
- require appropriate confirmation
- use appropriate authorization

50. CLEAN ARCHITECTURE Where appropriate, establish clear boundaries:

```text
UI
↓
Application logic
↓
Domain/business logic
↓
Infrastructure/data access
```

Do not force textbook architecture everywhere. Use the simplest architecture
that keeps responsibilities clear. 51. REFACTORING RULE When refactoring: DO:

- preserve behavior
- improve correctness
- reduce duplication
- improve readability
- improve types
- improve testability
- improve maintainability

DON'T:

- rewrite working code merely for style
- introduce unnecessary abstractions
- change APIs unnecessarily
- introduce huge dependencies
- create unnecessary layers

52. GIT / CHANGE SAFETY Before making large changes: Understand the current
    implementation. Make changes incrementally. After each major group of
    changes:

- re-check imports
- re-check types
- re-check routes
- re-check API contracts
- re-check UI references
- re-check database usage
- re-check tests

Do not create a situation where one giant change makes debugging impossible. 53.
VERIFY AFTER MODIFICATIONS After making changes, perform another audit. Do NOT
assume: “I changed it, therefore it works.” Verify. Run available:

```text
lint
typecheck
tests
build
```

and other project-specific validation commands. If commands fail:

1. understand the failure
2. determine whether it was pre-existing
3. fix it if related
4. rerun validation
5. BUILD VERIFICATION The application must build successfully. Check for:

- compile errors
- type errors
- missing imports
- broken routes
- missing assets
- environment problems
- bundler errors
- CSS problems
- dependency problems

Do not declare success if the build is broken. 55. DATABASE MIGRATION SAFETY If
database schema changes are needed:

- create proper migrations
- preserve existing data
- avoid destructive changes unless necessary
- consider existing production records
- maintain backward compatibility where appropriate

Never casually drop production data. 56. BACKWARD COMPATIBILITY When changing:

- APIs
- database schema
- URLs
- state formats
- stored data

consider whether existing users/data will break. Prefer safe migrations. 57.
SECURITY PRIORITY LEVELS Classify security issues: CRITICAL Immediate compromise
or severe data exposure. HIGH Significant security weakness. MEDIUM Meaningful
but limited vulnerability. LOW Hardening opportunity. Fix Critical and High
issues first. 58. BUG PRIORITY Classify bugs: P0 Application unusable / severe
data corruption / security failure. P1 Major functionality broken. P2 Important
but non-critical. P3 Minor issue. Prioritize appropriately. 59. UI/UX PRIORITY
Classify UI problems: Critical UX Prevents users from completing workflows.
Major UX Creates significant confusion/friction. Minor UX Polish issue.
Enhancement Nice-to-have. Focus first on usability, hierarchy, consistency, and
clarity. 60. DO NOT MAKE UP REQUIREMENTS Do not invent business requirements. If
something is ambiguous:

- infer cautiously from existing code
- inspect surrounding implementation
- inspect naming
- inspect existing UI
- inspect database schema
- inspect tests

If it truly cannot be determined, preserve existing behavior and clearly
document the ambiguity. 61. DO NOT REMOVE FUNCTIONALITY WITHOUT A REASON Do not
delete a feature merely because you dislike its implementation. If something
should be removed:

- establish why
- check dependencies
- check whether UI/API/database uses it
- ensure no hidden workflow depends on it

62. DO NOT BREAK EXISTING FEATURES After UI changes, ensure:

- API calls still work
- forms still submit
- navigation still works
- authentication still works
- permissions still work
- tables still work
- filters still work
- exports still work
- calculations remain accurate

UI improvement must not become a functional regression. 63. VISUAL CONSISTENCY
AUDIT Compare all major screens. Check consistency of:

- page headers
- cards
- buttons
- forms
- tables
- modals
- badges
- tabs
- navigation
- spacing
- typography
- icons
- colors
- states

Create reusable components where repeated patterns genuinely exist. 64.
COMPONENT ARCHITECTURE Avoid:

```text
One giant Dashboard.tsx
```

if the application is large. Break large components into meaningful pieces. But
don't create:

```text
ButtonWrapper
CardContainer
GenericLayoutBox
```

for every trivial element. Use abstractions that represent actual concepts. 65.
PERFORMANCE VS ABSTRACTION Do not optimize prematurely. But address obvious
issues such as:

- repeated expensive calculations
- unnecessary network calls
- huge lists
- unnecessary rerenders
- inefficient queries

Use measurement or strong reasoning where possible. 66. MOBILE UX For mobile,
prioritize:

- critical actions
- readable data
- navigation
- forms
- filtering
- responsive tables
- touch targets

Do not simply hide desktop content. Determine what mobile users actually
need. 67. SECURITY-FOCUSED FRONTEND REVIEW Remember: Everything shipped to the
browser is potentially observable by the user. Never put secrets in:

- frontend environment variables
- bundled JS
- source maps
- local storage
- client-side configuration

unless the value is intentionally public. 68. LOCAL STORAGE / COOKIES Audit
storage of:

- authentication tokens
- preferences
- financial data
- user information

Use appropriate secure mechanisms. Consider:

- HttpOnly
- Secure
- SameSite
- expiration
- encryption where appropriate

based on the authentication architecture. 69. THIRD-PARTY INTEGRATIONS Audit
every external service. For each:

- credentials
- error handling
- timeouts
- retries
- rate limits
- validation
- data exposure
- failure behavior

External services must not be allowed to take down the entire application
unnecessarily. 70. RESILIENCE Consider failures such as:

```text
Database unavailable
External API unavailable
Network timeout
Malformed response
Expired token
Rate limit
Large upload
Corrupt document
Invalid configuration
```

The application should fail gracefully. 71. FINAL QUALITY BAR Do not stop at:
“The code seems okay.” The final quality bar is: Correct The software behaves
correctly. Secure Reasonable security controls are implemented. Maintainable
Future developers can understand it. Tested Critical behavior is verified.
Performant No obvious unnecessary bottlenecks. Accessible Users can navigate and
operate it effectively. Responsive The UI works across relevant screen sizes.
Professional The product looks credible and polished. Consistent Patterns are
reused appropriately. Reliable Failures are handled gracefully. 72. REQUIRED
WORKFLOW Follow this sequence: PHASE 1 — DISCOVER Inspect the entire project. Do
not modify yet. Understand the architecture. PHASE 2 — AUDIT Identify:

- bugs
- security problems
- architecture problems
- performance issues
- UX problems
- accessibility issues
- testing gaps
- data integrity risks

PHASE 3 — PRIORITIZE Rank issues:

```text
Critical
High
Medium
Low
Enhancement
```

PHASE 4 — IMPLEMENT Fix the highest-value problems first. Implement changes
directly. PHASE 5 — UI/UX Perform the visual and interaction redesign. Improve:

- layout
- hierarchy
- spacing
- typography
- colors
- components
- tables
- forms
- navigation
- loading
- errors
- empty states
- responsiveness
- accessibility

PHASE 6 — TEST Run:

```text
lint
typecheck
unit tests
integration tests
build
```

plus any relevant project-specific checks. PHASE 7 — RE-AUDIT Inspect your own
modifications. Look for regressions. Verify that fixes did not introduce new
problems. PHASE 8 — FINAL REPORT Provide a concise but comprehensive final
report. 73. FINAL REPORT FORMAT At the end, report: Executive Summary Describe
the overall state of FinAuditPro. Example:

```text
Overall status:
Production-ready / Nearly production-ready / Significant work required
```

Architecture Explain:

- current architecture
- strengths
- weaknesses
- changes made

Bugs Found Use:

```text
Severity | Problem | Location | Fix
```

Security Findings Use:

```text
Severity | Finding | Risk | Remediation
```

Financial Logic Explain:

- calculations reviewed
- issues found
- fixes made
- remaining uncertainty

UI/UX Improvements Describe:

- dashboard
- navigation
- forms
- tables
- typography
- spacing
- colors
- responsive design
- accessibility
- states
- components

Performance Describe:

- major bottlenecks
- fixes
- remaining opportunities

Testing Report exactly what was run. Example:

```text
✓ Typecheck
✓ Lint
✓ Unit tests
✓ Build
✗ Integration tests — existing failure in X
```

Do not claim tests were run if they were not. Remaining Risks List anything that
could not be verified. Be honest. Recommended Next Steps Give only high-value
recommendations. 74. IMPORTANT: ACTUAL CODE MODIFICATION You have permission to
modify the codebase. Do not merely provide hypothetical code snippets. When you
identify a clear issue: fix it. When you identify a clear UI/UX improvement:
implement it. When you identify a clear security vulnerability: remediate it.
When you identify a clear code-quality problem: refactor it when the risk/reward
is justified. 75. IMPORTANT: PRESERVE BUSINESS LOGIC Do not change business
behavior merely because you prefer a different implementation. Before modifying
business logic:

1. understand the existing behavior
2. determine whether it is correct
3. identify dependencies
4. preserve valid behavior
5. modify only what is necessary
6. IMPORTANT: FINANCIAL DATA INTEGRITY Treat financial data as high-integrity
   information. Never introduce a UI optimization or frontend calculation that
   can silently change financial values. The backend/database should remain
   authoritative where appropriate.
7. IMPORTANT: SECURITY OVER CONVENIENCE Never weaken:

- authorization
- validation
- session security
- data isolation
- file access control

just to make a workflow easier. 78. IMPORTANT: NO FAKE COMPLETION Never say:
“Everything is perfect.” unless you genuinely verified it. Never claim:

- tests passed
- build passed
- vulnerability fixed
- UI tested
- API verified

unless you actually performed the relevant verification. Clearly distinguish:

```text
Verified
Likely correct
Not verified
Cannot determine
```

79. ENGINEERING MINDSET Think like a senior engineer receiving this repository
    six months before a production launch. Ask: “If thousands of real users
    depended on this application tomorrow, what would break?” Then find those
    problems. Ask: “If an attacker intentionally tried to abuse this
    application, where would they look?” Then investigate those areas. Ask: “If
    an auditor challenged the correctness of this financial data, can we explain
    exactly how it was produced?” Then inspect the data flow. Ask: “If a new
    developer joined the team tomorrow, could they safely modify this code?”
    Then improve maintainability. Ask: “If a finance professional used this
    every day, would the UI make their work faster or slower?” Then improve the
    UX.
80. FINAL INSTRUCTION Do not rush. Do not make superficial changes. Do not
    blindly rewrite the project. Do not blindly trust existing implementation.
    Do not optimize for number of changed lines. Optimize for: correctness +
    security + reliability + maintainability + performance + professional UX.
    Inspect first. Reason carefully. Modify confidently when justified. Test
    everything you can. Then perform a second review of your own work. The final
    result should make FinAuditPro feel like a serious production-grade
    financial auditing platform, not a prototype. Start by inspecting the entire
    repository and building a complete understanding of the system before making
    major changes.
